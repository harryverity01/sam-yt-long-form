[project]
name = "sam-yt-skills"
version = "0.1.0"
description = "Viral long-form YouTube pipeline as Claude Code skills"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "numpy>=1.26.0",
    "scipy>=1.11.0",
    "python-dotenv>=1.0.0",
    "Pillow>=10.0.0",
]

[tool.uv]
package = false
